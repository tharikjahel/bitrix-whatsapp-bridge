# ─── Evolution API ──────────────────────────────────────────────────────────
# URL base do seu servidor Evolution API (sem barra final)
EVOLUTION_API_URL=http://localhost:8080
# API Key global do Evolution (definida no .env do Evolution, campo AUTHENTICATION_API_KEY)
EVOLUTION_API_KEY=your-evolution-global-api-key

# ─── Bitrix24 Local App ─────────────────────────────────────────────────────
# Application ID e Secret gerados ao criar o App Local no Bitrix24
# (Configurações → Área do Desenvolvedor → Outros → Aplicativos locais)
BITRIX24_APP_ID=local.YOUR_APP_ID_HERE
BITRIX24_APP_SECRET=YOUR_APP_SECRET_HERE

# ID do conector — identificador único, apenas letras minúsculas, números e underscores
BITRIX24_CONNECTOR_ID=whatsapp_evolution

# application_token da autenticação do app (aparece nos eventos enviados pelo Bitrix24)
# Preencha após instalar o app e receber o primeiro evento
BITRIX24_APPLICATION_TOKEN=

# ─── Instâncias ────────────────────────────────────────────────────────────
# As instâncias são configuradas no arquivo instances.json (não aqui).
# Cada instância mapeia:  nome → instância Evolution + Linha Aberta do Bitrix24
# Veja instances.json para detalhes.

# ─── App ────────────────────────────────────────────────────────────────────
# URL pública do seu VPS onde esse app está rodando (sem barra final)
APP_URL=https://seu-dominio.com
APP_HOST=0.0.0.0
APP_PORT=8000
